[package]
name = "radish"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.12.0"
chrono = "0.4.45"
tokio = { version = "1.52.3",  features = ["macros", "rt", "io-util", "net"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true