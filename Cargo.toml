[package]
name = "radish"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45"
thiserror = "2.0.19"
tokio = { version = "1.52.3",  features = ["macros", "rt", "io-util", "net", "time"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
